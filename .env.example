# Supabase REST API Configuration (Using Supabase Database)
# Get these from: https://app.supabase.com
# 1. Go to Project Settings → API
# 2. Copy the Project URL (SUPABASE_URL)
# 3. Go to Project Settings → API → Project API keys
# 4. Use the anon key (SUPABASE_API_KEY)
#
# NOTE: We use REST API instead of direct database connection for better security
# and compatibility with serverless environments (GitHub Actions)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_API_KEY=your-supabase-anon-key-here

# Telegram Bot Configuration
# Get TELEGRAM_BOT_TOKEN from: @BotFather (via Telegram)
# Instructions: https://core.telegram.org/bots#6-botfather
TELEGRAM_BOT_TOKEN=your-bot-token-here

# Telegram Chat Configuration (for /set /list /clear commands)
# Get your personal chat ID from: @userinfobot (via Telegram)
# This is used when users message the bot directly
TELEGRAM_CHAT_ID=your-personal-chat-id-here

# Telegram Channel Configuration (Optional - for periodic notifications)
# If set, ALL bot notifications will be sent to this channel instead of individual users
# Get channel ID from: Send a message in your channel, then use @userinfobot to get the channel ID
# Format: -100xxxxxxxxxx (for private channels) or @channel_name (for public channels)
# Leave empty to send notifications to individual chats
TELEGRAM_NOTIFICATION_CHANNEL_ID=

# Application Configuration
CONFIG_PATH=./config.json
LOG_LEVEL=INFO

# Telegram Bot Backend Configuration
# These settings control the periodic search and notification behavior
BOT_CHECK_INTERVAL_MINUTES=15        # How often to check subscriptions (minutes)
BOT_ENABLED=true                      # Enable/disable the bot
BOT_ALLOWED_CHATS=                    # Optional: comma-separated chat IDs (leave empty to allow all)

# ============================================================================
# SETUP INSTRUCTIONS:
# ============================================================================
# 1. Copy this file to .env or .env.local:
#    cp .env.example .env.local
#
# 2. Edit .env.local with your actual credentials:
#    - DB_USER: postgres (default Supabase user)
#    - DB_PASSWORD: Your Supabase database password
#    - DB_HOST: Your Supabase host (db.xxx.supabase.co)
#    - DB_PORT: 5432 (default PostgreSQL port)
#    - DB_NAME: postgres (default Supabase database name)
#    - TELEGRAM_BOT_TOKEN: From @BotFather on Telegram
#    - TELEGRAM_CHAT_ID: Your Telegram chat ID
#
# 3. For GitHub Actions deployment, set these as GitHub Secrets:
#    Go to: Repository Settings → Secrets and Variables → Actions
#    Add these 5 database secrets + 2 Telegram secrets:
#    - DB_USER: postgres
#    - DB_PASSWORD: Your Supabase password
#    - DB_HOST: Your Supabase host
#    - DB_PORT: 5432
#    - DB_NAME: postgres
#    - TELEGRAM_BOT_TOKEN: Your Telegram bot token
#    - TELEGRAM_CHAT_ID: Your Telegram chat ID
#
# 4. NEVER commit actual credentials to git!
#    .env and .env.local are in .gitignore for safety
# ============================================================================

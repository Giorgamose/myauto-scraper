# Supabase PostgreSQL Database Configuration
# Get these from: https://app.supabase.com
# 1. Create a project or select existing project
# 2. Go to Settings → Database → Connection info
# 3. Extract the connection parameters:
#    - Host: db.[project-id].supabase.co
#    - User: postgres
#    - Password: Your Supabase password
#    - Port: 5432
#    - Database: postgres
#
# IMPORTANT: Use the individual parameters below (NOT a connection string).
# This approach is more reliable for serverless deployments (GitHub Actions, etc.)
DB_USER=postgres
DB_PASSWORD=your-password-here
DB_HOST=db.your-project-id.supabase.co
DB_PORT=5432
DB_NAME=postgres

# Telegram Bot Configuration
# Get TELEGRAM_BOT_TOKEN from: @BotFather (via Telegram)
# Get TELEGRAM_CHAT_ID from: @userinfobot (via Telegram)
TELEGRAM_BOT_TOKEN=your-bot-token-here
TELEGRAM_CHAT_ID=your-chat-id-here

# Application Configuration
CONFIG_PATH=./config.json
LOG_LEVEL=INFO

# ============================================================================
# SETUP INSTRUCTIONS:
# ============================================================================
# 1. Copy this file to .env or .env.local:
#    cp .env.example .env.local
#
# 2. Edit .env.local with your actual credentials:
#    - DB_USER: postgres (default Supabase user)
#    - DB_PASSWORD: Your Supabase database password
#    - DB_HOST: Your Supabase host (db.xxx.supabase.co)
#    - DB_PORT: 5432 (default PostgreSQL port)
#    - DB_NAME: postgres (default Supabase database name)
#    - TELEGRAM_BOT_TOKEN: From @BotFather on Telegram
#    - TELEGRAM_CHAT_ID: Your Telegram chat ID
#
# 3. For GitHub Actions deployment, set these as GitHub Secrets:
#    Go to: Repository Settings → Secrets and Variables → Actions
#    Add these 5 database secrets + 2 Telegram secrets:
#    - DB_USER: postgres
#    - DB_PASSWORD: Your Supabase password
#    - DB_HOST: Your Supabase host
#    - DB_PORT: 5432
#    - DB_NAME: postgres
#    - TELEGRAM_BOT_TOKEN: Your Telegram bot token
#    - TELEGRAM_CHAT_ID: Your Telegram chat ID
#
# 4. NEVER commit actual credentials to git!
#    .env and .env.local are in .gitignore for safety
# ============================================================================
